name: Go

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Criando go.mod
      run: go mod init danielCI

    - name: Build
      run: go build main.go

  # docker:
  #   needs: app
  #   uses: ./.github/workflows/docker.yml